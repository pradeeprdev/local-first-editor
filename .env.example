# Copy this file to .env and fill in real values.

# Local Postgres (see README for Mac setup via Postgres.app or Docker)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/local_first_editor?schema=public"

# Any long random string - used to sign login session JWTs
JWT_SECRET="replace-with-a-long-random-string"
